{
  "name": "@stakewithfriends/contracts",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:username-registry": "hardhat run scripts/deploy-username-registry.js --network monadTestnet",
    "deploy": "hardhat run scripts/deploy-contracts.js --network monadTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.24.3",
    "solc": "^0.8.24"
  }
}
